version: 0.0
os: linux
files:
  - source: /scripts
    destination: /var/www/html

hooks:
  AfterInstall:
    - location: scripts/install.sh
      timeout: 600
      runas: root

  ValidateService:
    - location: scripts/start.sh
      timeout: 600
      runas: root
